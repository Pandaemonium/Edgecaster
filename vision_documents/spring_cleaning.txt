SPRING CLEANING — ACTIONABLE PLAN
(Current Status: Widget system online; PanelScene foundation online; VisualProfile + VisualEffect pipeline active)

Legend:
[CLEANUP] structure/no-behavior change
[BEHAVIOR] gameplay change
[VISION] long-term direction

===============================================================================
SLICE 1: Unified TargetMode [FOUNDATION COMPLETE]
✓ Tile / vertex / look targeting unified
✓ Mouse + keyboard symmetric
✓ Inspect mode uses entity inheritance

Next:
- Cone / line shapes
- Ranged preview layering
- Multi-step targeting for complex spells

===============================================================================
SLICE 2: Menu & UI System Rework [CLEANUP — ACTIVE]

Problem:
Menus, keybinds, inventory, and special scenes historically used ad-hoc layout
code and scene-specific hacks (especially for visuals and mouse hit-testing).

New foundation (now real):
✓ PanelScene: panel-local rendering + root widget tree + UI-first input routing
✓ GeneralMenuScene: selection model + confirm/cancel + scrolling
✓ Legacy wrappers: MenuScene and PopupMenuScene now wrap GeneralMenuScene
✓ VisualProfile / VisualEffects apply consistently via visuals.py + visual_effects.py
✓ Mouse hit-testing works under rotation/scale via unproject_mouse(...)
✓ Multi-line banners restored via MultiLineLabelWidget (urgent/dialogue headers)

Important gotchas learned (don’t regress):
- Init ordering: GeneralMenuScene builds widgets during __init__ and may call
  get_ascii_art() early. Subclasses MUST set fields used by get_ascii_art()
  BEFORE calling super().__init__().
- Import hygiene: avoid circular imports (events ↔ scenes ↔ renderer). Prefer:
    from edgecaster.scenes.manager import SceneManager
  and avoid importing SceneManager from edgecaster.scenes package root.

Temporary scaffolding (do not let this linger):
- dungeon.py still contains an F8 debug overlay used to validate widget input
  interception. Keep as fallback until the widget-based HUD is stable across
  Dungeon + Inventory + Popup flows, then remove.

Migration order (stay disciplined):
HUD → widget in its own file (overlay layer)  [DONE]
Ability Bar → widgetized + preserve clickiness  [DONE]
MenuScene/PopupMenuScene → wrappers over GeneralMenuScene  [DONE]
InventoryScene/UrgentMessageScene/Dialogue → still “legacy wrapper” scenes; functional parity first
Next: Make menus less ugly (spacing/borders/fonts), then two-pane inventory.

Near-term to-do (UI polish / correctness):
[CLEANUP] Add basic panel chrome (border + title region) for popups
[CLEANUP] Standardize padding + font choices across menus
[CLEANUP] ListWidget: consistent scroll behavior + ensure_visible on selection
[CLEANUP] Footer clipping: reserve footer height reliably in layouts
[CLEANUP] Make urgent messages & dialogue use the same “banner” conventions (title/body above choices)
[CLEANUP] Remove duplicate/legacy methods in urgent/dialogue scenes (wrap_text variants, old _draw_panel paths)

===============================================================================
SLICE 3: Visual Effects & Presentation [FOUNDATION STRONG]

Visual effects milestone (expanded):
✓ visual_effects.py is the single registry for named effects
✓ Effects support multiple “lanes”:
  - geometry lane (VisualProfile transforms)
  - color lane (tint/flicker)
  - present lane (shake/vibrate impulses)
  - overlay lane (particles/shimmer/scanlines drawn onto surfaces)
✓ Effects can define overlay bounds (local rect) to localize auras/smoke/embers
  around entities/tiles or within panels, preventing whole-screen bleed.
✓ Effects apply uniformly across scenes, popups, and entities
✓ Duplicate effects intentionally compound (fractal recursion)

Next:
[CLEANUP] Define a consistent default “menu background” and popup dim level
[CLEANUP] Create a small library of reusable panel styles (fullscreen menu, small popup, wide popup)

===============================================================================
SLICE 4: Data-Driven Everything [VISION]

Expand prototypes with:
- materials, physiology, resistances
- behavior components (curious, brittle, fractal, volatile)
- conditions/adjectives system that attaches gameplay + visual effects
  (e.g. "Fiery" condition → VisualEffect + resistances)

Longer-term:
- descriptions/names generated dynamically from trait stacks
- avoid hardcoded prefixes once the system exists

===============================================================================
ONGOING ENGINE CLEANUPS

[CLEANUP] Decompose game.py into systems/*
[CLEANUP] Scheduler/event semantics cleanup
[CLEANUP] MessageLog performance (deque)
[CLEANUP] Dataclass defaults & typing consistency
[CLEANUP] Actor/entity identification consistency
[CLEANUP] Level registry invariants
[CLEANUP] Kill-path ID consistency
[CLEANUP] Overmap thread safety
[CLEANUP] Centralize tuning constants
[CLEANUP] Inventory/entity lookup optimizations
[CLEANUP] Actor/entity position indexes
[CLEANUP] Normalize content-file resolution
[CLEANUP] Remove leftover debug scaffolding
[CLEANUP] Explicit initialization pipeline
[CLEANUP] Tighten scheduler typing
[CLEANUP] Unify ignite/regrow effect logic
[CLEANUP] Distinguish movement vs LOS blocking

Guiding principles:
Keep the geometry weird.
Keep responsibilities clean.
Refactor early, but surgically.
